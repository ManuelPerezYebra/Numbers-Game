const root = document.documentElement;
const fisrtNumberElement = document.getElementById('fisrtnumber');
const scoreElement = document.getElementById('score');
const secondNumberElement = document.getElementById('secondnumber');
const operatorElement = document.getElementById('operator');
const resultoptionsElement = document.getElementById('options');
const darkmodeElement = document.getElementById('sun');
const darkmodeChange = document.getElementById('darkmodeicon');
const satartGameButton = document.getElementById('startGame');
const sound = document.querySelector('audio');
const totalPointsElement = document.getElementById('totalpointsElement');
const totaloperationsElement = document.getElementById('totaloperations');
const totalpointscontainer = document.getElementById('totalpointscontainer');
const roundCounter = document.getElementById('round');
const audioElement = document.getElementById('audio');

export {
	root,
	fisrtNumberElement,
	scoreElement,
	secondNumberElement,
	operatorElement,
	resultoptionsElement,
	darkmodeElement,
	darkmodeChange,
	satartGameButton,
	sound,
	totalPointsElement,
	totaloperationsElement,
	totalpointscontainer,
	roundCounter,
	audioElement
};
